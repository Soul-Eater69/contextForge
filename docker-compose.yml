services:
  context-forge:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "4000:4000"
    environment:
      HOST: 0.0.0.0
      PORT: 4000
      DATA_DIR: /app/data
      PUBLIC_DIR: /app/public
      AUTO_SEED_DEMO: "false"
    volumes:
      - ./data:/app/data

